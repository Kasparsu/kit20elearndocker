version: '3.8'

services:
  app:
    build:
      dockerfile: ./app.dockerfile
      context: .
    ports:
      - 80:80
      # - 443 equal to 443:443
    volumes:
      - ./htdocs:/var/www/html
      - ./site1:/var/www/site1
      - ./site2:/var/www/site2